const App = () => (
  <div>

    const BasicButton = () => (
      return <button>Click Me</button>;
    );

    const BasicInput = () => (
      return <input type="text"
    )


  </div>
);

ReactDOM.render(<App />, document.getElementById("root"));
